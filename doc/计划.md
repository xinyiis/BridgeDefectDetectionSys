方案1实施计划：让 StartTask 直接触发处理
核心问题分析
当前架构的问题：

StartTask 只改状态为 ready_to_process，不启动处理
StreamTask 通过 WebSocket 连接才调用 runTask
runTask 依赖 send 函数推送进度（WebSocket）
没有 WebSocket 时，任务永远停留在 ready_to_process
设计方案
方案概述
在 StartTask 中启动一个后台 goroutine 调用 runTask，不依赖 WebSocket 连接。进度通过算法回调更新到数据库，客户端通过轮询 GetTask 获取状态。

关键修改点
1. 修改 StartTask 方法


func (uc *VideoDetectionUseCase) StartTask(taskID string, currentUser *model.User) (*dto.VideoStartResponse, error) {
    task, err := uc.loadOwnedTask(taskID, currentUser)
    if err != nil {
        return nil, err
    }
    
    if task.Status != model.VideoTaskUploaded {
        return nil, errors.New("当前任务状态不允许开始分析")
    }
    
    // 防止重复启动
    if _, loaded := uc.running.LoadOrStore(task.TaskID, struct{}{}); loaded {
        return nil, errors.New("任务已在处理中")
    }
    
    task.SessionID = "session_" + uuid.NewString()
    task.Status = model.VideoTaskReadyToProcess
    task.ErrorMessage = ""
    task.CanceledAt = nil
    task.CompletedAt = nil
    task.ProcessedFrames = 0
    task.ConfirmedDefects = 0
    
    if err := uc.taskRepo.Update(task); err != nil {
        uc.running.Delete(task.TaskID)  // 回滚
        return nil, fmt.Errorf("更新任务状态失败: %w", err)
    }
    
    uc.tasks.Store(task.TaskID, task)
    
    // 🔥 新增：启动后台处理 goroutine
    go func() {
        defer uc.running.Delete(task.TaskID)
        
        // 创建一个空的 runtime（不需要 WebSocket send）
        runtime := uc.ensureRuntime(task.TaskID, nil)
        
        // 调用 runTask 处理
        if err := uc.runTask(task, nil); err != nil {
            log.Printf("[VideoDetection] Task %s failed: %v", task.TaskID, err)
        }
    }()
    
    return &dto.VideoStartResponse{
        TaskID:    task.TaskID,
        SessionID: task.SessionID,
        Status:    task.Status,
        WSURL:     fmt.Sprintf("/api/v1/detection/video/ws?task_id=%s&session_id=%s", task.TaskID, task.SessionID),
    }, nil
}
2. 修改 StreamTask 方法


func (uc *VideoDetectionUseCase) StreamTask(taskID, sessionID string, currentUser *model.User, send func(any) error) error {
    task, err := uc.loadOwnedTask(taskID, currentUser)
    if err != nil {
        return err
    }
    if task.SessionID == "" || task.SessionID != sessionID {
        return errors.New("分析会话不存在")
    }
    
    uc.sendMessage(send, dto.VideoConnectedMessage{
        Type:      "connected",
        TaskID:    task.TaskID,
        SessionID: task.SessionID,
        Message:   "websocket connected",
    })
    
    // 🔥 修改：获取或创建 runtime，更新 send 函数
    runtime := uc.ensureRuntime(task.TaskID, send)
    runtime.mu.Lock()
    runtime.send = send  // 更新 WebSocket 发送函数
    runtime.mu.Unlock()
    
    switch task.Status {
    case model.VideoTaskReadyToProcess:
        // 🔥 修改：任务已在后台处理，只需等待完成
        <-runtime.done
        return nil
    case model.VideoTaskFrameExtracting, model.VideoTaskDispatching, model.VideoTaskQueued:
        <-runtime.done
        return nil
    case model.VideoTaskProcessing:
        <-runtime.done
        return nil
    case model.VideoTaskCompleted:
        uc.sendMessage(send, dto.VideoCompletedMessage{...})
        return nil
    case model.VideoTaskCanceled:
        return errors.New("任务已取消")
    default:
        return errors.New("当前任务状态不允许建立分析流")
    }
}
3. 修改 sendMessage 方法（已经是安全的）


func (uc *VideoDetectionUseCase) sendMessage(send func(any) error, message any) {
    if send == nil {
        return  // ✅ 无 WebSocket 时直接返回，不推送
    }
    _ = send(message)
}
4. 修改 ensureRuntime 方法


func (uc *VideoDetectionUseCase) ensureRuntime(taskID string, send func(any) error) *videoTaskRuntime {
    if existing := uc.loadRuntime(taskID); existing != nil {
        // 🔥 修改：如果 send 不为 nil，更新 send 函数
        if send != nil {
            existing.mu.Lock()
            existing.send = send
            existing.mu.Unlock()
        }
        return existing
    }
    
    runtime := &videoTaskRuntime{
        send:    send,  // 可以为 nil
        summary: make(map[string]int),
        done:    make(chan struct{}),
    }
    uc.runtimes.Store(taskID, runtime)
    return runtime
}
实施步骤
第1步：修改 video_detection_usecase.go
修改 StartTask 方法，添加后台 goroutine
修改 StreamTask 方法，移除 runTask 调用
修改 ensureRuntime 方法，支持动态更新 send
第2步：测试验证
单元测试：验证 StartTask 后任务自动处理
集成测试：验证无 WebSocket 连接时任务正常完成
端到端测试：运行测试脚本验证完整流程
第3步：清理测试脚本
移除 WebSocket 连接相关代码
保持轮询逻辑
优势
✅ REST API 语义正确：POST /start 真正启动任务
✅ 无需 WebSocket：客户端只需轮询状态
✅ 向后兼容：WebSocket 仍可用于实时进度推送
✅ 简化客户端：不需要维护 WebSocket 连接
✅ 更健壮：网络断开不影响任务处理
风险和注意事项
并发控制：uc.running 已经防止重复启动 ✅
错误处理：goroutine 中的错误需要记录到数据库 ✅
资源清理：defer 语句确保清理 ✅
进度推送：无 WebSocket 时不推送，只更新数据库 ✅
测试计划

# 1. 启动服务
cd src/backend && go run cmd/main.go

# 2. 运行测试脚本（无需 WebSocket）
./test_video_detection_e2e.sh /path/to/test_video.mp4

# 3. 验证任务完成
curl http://localhost:8080/api/v1/detection/video/{task_id} \
  -b /tmp/session_cookie.txt | jq '.data.status'
# 期望输出: "completed"