# 主页统计模块开发计划（方案2：内存缓存）

## 文档信息

- **模块名称**: 主页信息统计模块
- **版本**: v1.0
- **创建日期**: 2026-03-17
- **预计总耗时**: 3.5小时
- **技术方案**: MySQL索引优化 + Go内存缓存
- **依赖模块**: 用户、桥梁、无人机、缺陷检测（均已完成）

---

## 目录

1. [总体规划](#1-总体规划)
2. [阶段0：准备工作](#2-阶段0准备工作)
3. [阶段1：DTO定义](#3-阶段1dto定义)
4. [阶段2：内存缓存实现](#4-阶段2内存缓存实现)
5. [阶段3：Service层实现](#5-阶段3service层实现)
6. [阶段4：UseCase层实现](#6-阶段4usecase层实现)
7. [阶段5：Handler + 路由](#7-阶段5handler--路由)
8. [阶段6：数据库索引优化](#8-阶段6数据库索引优化)
9. [阶段7：测试编写](#9-阶段7测试编写)
10. [阶段8：提交代码](#10-阶段8提交代码)

---

## 1. 总体规划

### 1.1 开发阶段概览

| 阶段 | 任务名称 | 预计耗时 | 累计耗时 | 优先级 |
|------|---------|---------|---------|--------|
| 阶段0 | 准备工作 | 5分钟 | 5分钟 | P0 |
| 阶段1 | DTO定义 | 20分钟 | 25分钟 | P0 |
| 阶段2 | 内存缓存实现 | 30分钟 | 55分钟 | P0 |
| 阶段3 | Service层实现 | 1小时 | 1小时55分 | P0 |
| 阶段4 | UseCase层实现 | 20分钟 | 2小时15分 | P0 |
| 阶段5 | Handler + 路由 | 30分钟 | 2小时45分 | P0 |
| 阶段6 | 数据库索引优化 | 10分钟 | 2小时55分 | P0 |
| 阶段7 | 测试编写 | 30分钟 | 3小时25分 | P0 |
| 阶段8 | 提交代码 | 5分钟 | 3小时30分 | P0 |

**总计**: 约 **3.5小时**

### 1.2 技术栈

```
已有技术栈:
- Go 1.21+
- Gin Web框架
- GORM ORM
- MySQL 8.0
- Session认证

本次新增:
- sync.RWMutex (Go标准库)
- 内存缓存实现（自研）
```

### 1.3 依赖关系图

```
阶段0 (准备)
    ↓
阶段1 (DTO) ← 定义数据结构
    ↓
阶段2 (内存缓存) ← 独立模块
    ↓
阶段3 (Service) ← 依赖DTO + 缓存
    ↓
阶段4 (UseCase) ← 依赖Service
    ↓
阶段5 (Handler) ← 依赖UseCase
    ↓
阶段6 (索引优化) ← 独立任务
    ↓
阶段7 (测试) ← 依赖所有层
    ↓
阶段8 (提交)
```

---

## 2. 阶段0：准备工作

### 2.1 任务清单

- [ ] 阅读《主页统计模块详细设计.md》
- [ ] 确认已有模块数据表结构
- [ ] 验证测试环境MySQL可用

### 2.2 预计时间

**5分钟**

### 2.3 验收标准

- ✅ 理解6个统计接口的业务逻辑
- ✅ 确认 `users`、`bridges`、`drones`、`defects` 表存在
- ✅ MySQL连接正常

### 2.4 执行命令

```bash
# 1. 阅读设计文档
cat doc/主页统计模块详细设计.md

# 2. 检查数据表
mysql -uroot -p123456 -e "USE bridge_detection; SHOW TABLES;"

# 3. 查看表结构
mysql -uroot -p123456 -e "USE bridge_detection; DESCRIBE defects;"
```

---

## 3. 阶段1：DTO定义

### 3.1 任务清单

- [ ] 创建 `stats_dto.go` 文件
- [ ] 定义 `StatsOverview` 结构（概览统计）
- [ ] 定义 `DefectTypeDistribution` 结构（类型分布）
- [ ] 定义 `DefectTrend` 结构（趋势统计）
- [ ] 定义 `BridgeHealthRanking` 结构（桥梁排名）
- [ ] 定义 `RecentDetection` 结构（最近检测）
- [ ] 定义 `HighRiskAlert` 结构（高危告警）
- [ ] 定义响应封装结构

### 3.2 预计时间

**20分钟**

### 3.3 文件路径

```
internal/application/dto/stats_dto.go
```

### 3.4 代码结构

```go
// StatsOverview 概览统计
type StatsOverview struct {
    BridgeCount      int     `json:"bridge_count"`
    DroneCount       int     `json:"drone_count"`
    DefectCount      int     `json:"defect_count"`
    DetectionCount   int     `json:"detection_count"`
    TodayDefects     int     `json:"today_defects"`
    WeekDefects      int     `json:"week_defects"`
    DefectTrend      float64 `json:"defect_trend"`
    TrendDirection   string  `json:"trend_direction"`
}

// DefectTypeDistribution 缺陷类型分布
type DefectTypeDistribution struct {
    DefectType    string  `json:"defect_type"`
    Count         int     `json:"count"`
    Percentage    float64 `json:"percentage"`
    AvgConfidence float64 `json:"avg_confidence"`
}

// DefectTypeDistributionResponse 类型分布响应
type DefectTypeDistributionResponse struct {
    Total        int                      `json:"total"`
    Distribution []DefectTypeDistribution `json:"distribution"`
}

// DefectTrend 缺陷趋势
type DefectTrend struct {
    Date            string `json:"date"`
    Count           int    `json:"count"`
    CumulativeCount int    `json:"cumulative_count"`
}

// DefectTrendResponse 趋势响应
type DefectTrendResponse struct {
    Period      string        `json:"period"`
    Granularity string        `json:"granularity"`
    Trend       []DefectTrend `json:"trend"`
    Total       int           `json:"total"`
    AvgPerDay   float64       `json:"avg_per_day"`
    PeakDate    string        `json:"peak_date"`
    PeakCount   int           `json:"peak_count"`
}

// BridgeHealthRanking 桥梁健康度排名
type BridgeHealthRanking struct {
    BridgeID      uint      `json:"bridge_id"`
    BridgeName    string    `json:"bridge_name"`
    DefectCount   int       `json:"defect_count"`
    HighRiskCount int       `json:"high_risk_count"`
    LastDetection time.Time `json:"last_detection"`
    HealthScore   float64   `json:"health_score"`
    HealthLevel   string    `json:"health_level"`
}

// BridgeRankingResponse 排名响应
type BridgeRankingResponse struct {
    Ranking []BridgeHealthRanking `json:"ranking"`
}

// RecentDetection 最近检测记录
type RecentDetection struct {
    DetectionID    uint      `json:"detection_id"`
    BridgeID       uint      `json:"bridge_id"`
    BridgeName     string    `json:"bridge_name"`
    ImagePath      string    `json:"image_path"`
    DefectCount    int       `json:"defect_count"`
    DetectedAt     time.Time `json:"detected_at"`
    ProcessingTime float64   `json:"processing_time"`
}

// RecentDetectionsResponse 最近检测响应
type RecentDetectionsResponse struct {
    Detections []RecentDetection `json:"detections"`
}

// HighRiskAlert 高危缺陷告警
type HighRiskAlert struct {
    DefectID   uint      `json:"defect_id"`
    BridgeID   uint      `json:"bridge_id"`
    BridgeName string    `json:"bridge_name"`
    DefectType string    `json:"defect_type"`
    Confidence float64   `json:"confidence"`
    Area       float64   `json:"area"`
    Severity   string    `json:"severity"`
    DetectedAt time.Time `json:"detected_at"`
    ImagePath  string    `json:"image_path"`
}

// HighRiskAlertsResponse 高危告警响应
type HighRiskAlertsResponse struct {
    Total  int             `json:"total"`
    Alerts []HighRiskAlert `json:"alerts"`
}
```

### 3.5 验收标准

- ✅ 所有DTO结构定义完整
- ✅ JSON标签正确
- ✅ 字段类型符合业务需求
- ✅ 代码编译通过：`go build ./...`

---

## 4. 阶段2：内存缓存实现

### 4.1 任务清单

- [ ] 创建 `memory_cache.go` 文件
- [ ] 实现线程安全的内存缓存
- [ ] 实现Get/Set/Del方法
- [ ] 实现TTL过期机制
- [ ] 实现通配符删除（批量失效）
- [ ] 创建全局缓存实例

### 4.2 预计时间

**30分钟**

### 4.3 文件路径

```
pkg/cache/memory_cache.go
```

### 4.4 代码实现

```go
// Package cache 实现内存缓存
package cache

import (
    "encoding/json"
    "strings"
    "sync"
    "time"
)

// MemoryCache 内存缓存（线程安全）
type MemoryCache struct {
    data map[string][]byte      // 存储序列化后的数据
    ttl  map[string]time.Time   // 存储过期时间
    mu   sync.RWMutex           // 读写锁
}

// NewMemoryCache 创建内存缓存实例
func NewMemoryCache() *MemoryCache {
    cache := &MemoryCache{
        data: make(map[string][]byte),
        ttl:  make(map[string]time.Time),
    }

    // 启动后台清理任务（每分钟清理过期数据）
    go cache.cleanupExpired()

    return cache
}

// Get 获取缓存（自动反序列化）
// 参数：
//   - key: 缓存键
//   - dest: 目标对象指针（用于反序列化）
// 返回：
//   - error: 缓存未命中或已过期返回错误
func (c *MemoryCache) Get(key string, dest interface{}) error {
    c.mu.RLock()
    defer c.mu.RUnlock()

    // 检查是否存在
    data, exists := c.data[key]
    if !exists {
        return ErrCacheMiss
    }

    // 检查是否过期
    if expireTime, ok := c.ttl[key]; ok {
        if time.Now().After(expireTime) {
            return ErrCacheExpired
        }
    }

    // 反序列化
    return json.Unmarshal(data, dest)
}

// Set 设置缓存（自动序列化）
// 参数：
//   - key: 缓存键
//   - value: 缓存值（任意类型，会被JSON序列化）
//   - ttl: 过期时间（0表示永不过期）
func (c *MemoryCache) Set(key string, value interface{}, ttl time.Duration) error {
    // 序列化
    data, err := json.Marshal(value)
    if err != nil {
        return err
    }

    c.mu.Lock()
    defer c.mu.Unlock()

    c.data[key] = data

    if ttl > 0 {
        c.ttl[key] = time.Now().Add(ttl)
    }

    return nil
}

// Del 删除缓存
// 参数：
//   - key: 缓存键
func (c *MemoryCache) Del(key string) {
    c.mu.Lock()
    defer c.mu.Unlock()

    delete(c.data, key)
    delete(c.ttl, key)
}

// DelPattern 删除匹配模式的所有缓存
// 支持通配符 * （例如：stats:overview:* 删除所有用户的概览统计）
// 参数：
//   - pattern: 匹配模式
func (c *MemoryCache) DelPattern(pattern string) int {
    c.mu.Lock()
    defer c.mu.Unlock()

    // 将通配符 * 替换为正则表达式
    pattern = strings.ReplaceAll(pattern, "*", "")

    count := 0
    for key := range c.data {
        if strings.Contains(key, pattern) {
            delete(c.data, key)
            delete(c.ttl, key)
            count++
        }
    }

    return count
}

// Clear 清空所有缓存
func (c *MemoryCache) Clear() {
    c.mu.Lock()
    defer c.mu.Unlock()

    c.data = make(map[string][]byte)
    c.ttl = make(map[string]time.Time)
}

// Size 获取缓存条目数量
func (c *MemoryCache) Size() int {
    c.mu.RLock()
    defer c.mu.RUnlock()

    return len(c.data)
}

// cleanupExpired 后台清理过期数据（每分钟执行一次）
func (c *MemoryCache) cleanupExpired() {
    ticker := time.NewTicker(1 * time.Minute)
    defer ticker.Stop()

    for range ticker.C {
        c.mu.Lock()
        now := time.Now()

        for key, expireTime := range c.ttl {
            if now.After(expireTime) {
                delete(c.data, key)
                delete(c.ttl, key)
            }
        }

        c.mu.Unlock()
    }
}

// 错误定义
var (
    ErrCacheMiss    = fmt.Errorf("cache miss")
    ErrCacheExpired = fmt.Errorf("cache expired")
)

// 全局缓存实例
var StatsCache = NewMemoryCache()

// 缓存键常量
const (
    CacheKeyOverview        = "stats:overview:%d"           // stats:overview:1 (用户ID)
    CacheKeyDefectTypes     = "stats:defect_types:%d:%d"    // stats:defect_types:1:30 (用户ID:天数)
    CacheKeyDefectTrend     = "stats:trend:%d:%d:%s"        // stats:trend:1:7:day (用户ID:天数:粒度)
    CacheKeyBridgeRanking   = "stats:ranking:%d:%d"         // stats:ranking:1:10 (用户ID:数量)
    CacheKeyRecentDetection = "stats:recent:%d:%d"          // stats:recent:1:10 (用户ID:数量)
    CacheKeyHighRiskAlert   = "stats:alerts:%d:%s:%d"       // stats:alerts:1:urgent:20 (用户ID:严重度:数量)
)

// TTL常量
const (
    CacheTTLOverview   = 5 * time.Minute  // 概览统计：5分钟
    CacheTTLDefectType = 10 * time.Minute // 类型分布：10分钟
    CacheTTLTrend      = 1 * time.Hour    // 趋势统计：1小时
    CacheTTLRanking    = 10 * time.Minute // 排名数据：10分钟
    CacheTTLRecent     = 3 * time.Minute  // 最近检测：3分钟
    CacheTTLAlert      = 5 * time.Minute  // 高危告警：5分钟
)
```

### 4.5 验收标准

- ✅ 线程安全（使用sync.RWMutex）
- ✅ 支持TTL自动过期
- ✅ 支持通配符删除
- ✅ 后台自动清理过期数据
- ✅ 代码编译通过：`go build ./pkg/cache`

---

## 5. 阶段3：Service层实现

### 5.1 任务清单

- [ ] 创建 `stats_service.go` 接口文件
- [ ] 创建 `stats_service_impl.go` 实现文件
- [ ] 实现 `GetOverview` 方法（概览统计）
- [ ] 实现 `GetDefectTypeDistribution` 方法（类型分布）
- [ ] 实现 `GetDefectTrend` 方法（趋势统计）
- [ ] 实现 `GetBridgeRanking` 方法（桥梁排名）
- [ ] 实现 `GetRecentDetections` 方法（最近检测）
- [ ] 实现 `GetHighRiskAlerts` 方法（高危告警）
- [ ] 实现权限过滤通用方法
- [ ] 集成内存缓存

### 5.2 预计时间

**1小时**

### 5.3 文件路径

```
internal/domain/service/stats_service.go          # 接口定义
internal/infrastructure/persistence/stats_service_impl.go  # 实现
```

### 5.4 接口定义

```go
// Package service 定义领域服务
package service

import (
    "github.com/xinyiis/BridgeDefectDetectionSys/src/backend/internal/application/dto"
    "github.com/xinyiis/BridgeDefectDetectionSys/src/backend/internal/domain/model"
)

// StatsService 统计服务接口
type StatsService interface {
    // GetOverview 获取用户概览统计
    GetOverview(currentUser *model.User) (*dto.StatsOverview, error)

    // GetDefectTypeDistribution 获取缺陷类型分布
    GetDefectTypeDistribution(currentUser *model.User, days int) (*dto.DefectTypeDistributionResponse, error)

    // GetDefectTrend 获取缺陷趋势统计
    GetDefectTrend(currentUser *model.User, days int, granularity string) (*dto.DefectTrendResponse, error)

    // GetBridgeRanking 获取桥梁健康度排名
    GetBridgeRanking(currentUser *model.User, limit int, order string) (*dto.BridgeRankingResponse, error)

    // GetRecentDetections 获取最近检测记录
    GetRecentDetections(currentUser *model.User, limit int) (*dto.RecentDetectionsResponse, error)

    // GetHighRiskAlerts 获取高危缺陷告警
    GetHighRiskAlerts(currentUser *model.User, severity string, limit int) (*dto.HighRiskAlertsResponse, error)
}
```

### 5.5 核心实现要点

#### 5.5.1 GetOverview方法（带缓存）
```go
func (s *StatsServiceImpl) GetOverview(currentUser *model.User) (*dto.StatsOverview, error) {
    // 1. 尝试从缓存获取
    cacheKey := fmt.Sprintf(cache.CacheKeyOverview, currentUser.ID)
    var overview dto.StatsOverview
    if err := cache.StatsCache.Get(cacheKey, &overview); err == nil {
        return &overview, nil // 缓存命中
    }

    // 2. 缓存未命中，查询数据库
    overview = dto.StatsOverview{}

    // 基础统计（子查询）
    if currentUser.IsAdmin() {
        s.db.Raw(`
            SELECT
                (SELECT COUNT(*) FROM bridges WHERE deleted_at IS NULL) AS bridge_count,
                (SELECT COUNT(*) FROM drones WHERE deleted_at IS NULL) AS drone_count,
                (SELECT COUNT(*) FROM defects WHERE deleted_at IS NULL) AS defect_count,
                (SELECT COUNT(DISTINCT image_path) FROM defects WHERE deleted_at IS NULL) AS detection_count
        `).Scan(&overview)
    } else {
        s.db.Raw(`
            SELECT
                (SELECT COUNT(*) FROM bridges WHERE user_id = ? AND deleted_at IS NULL) AS bridge_count,
                (SELECT COUNT(*) FROM drones WHERE user_id = ? AND deleted_at IS NULL) AS drone_count,
                (SELECT COUNT(d.id) FROM defects d
                 JOIN bridges b ON b.id = d.bridge_id
                 WHERE b.user_id = ? AND d.deleted_at IS NULL) AS defect_count,
                (SELECT COUNT(DISTINCT d.image_path) FROM defects d
                 JOIN bridges b ON b.id = d.bridge_id
                 WHERE b.user_id = ? AND d.deleted_at IS NULL) AS detection_count
        `, currentUser.ID, currentUser.ID, currentUser.ID, currentUser.ID).Scan(&overview)
    }

    // 增量统计（今日、本周、昨日）
    baseQuery := s.buildAuthorizedQuery(currentUser)

    var todayDefects, weekDefects, yesterdayDefects int64
    baseQuery.Where("DATE(defects.detected_at) = CURDATE()").Count(&todayDefects)
    baseQuery.Where("defects.detected_at >= DATE_SUB(CURDATE(), INTERVAL 7 DAY)").Count(&weekDefects)
    baseQuery.Where("DATE(defects.detected_at) = DATE_SUB(CURDATE(), INTERVAL 1 DAY)").Count(&yesterdayDefects)

    overview.TodayDefects = int(todayDefects)
    overview.WeekDefects = int(weekDefects)

    // 计算趋势
    if yesterdayDefects > 0 {
        overview.DefectTrend = float64(todayDefects-yesterdayDefects) / float64(yesterdayDefects) * 100
    }

    if todayDefects > yesterdayDefects {
        overview.TrendDirection = "up"
    } else if todayDefects < yesterdayDefects {
        overview.TrendDirection = "down"
    } else {
        overview.TrendDirection = "stable"
    }

    // 3. 写入缓存
    cache.StatsCache.Set(cacheKey, overview, cache.CacheTTLOverview)

    return &overview, nil
}
```

#### 5.5.2 权限过滤通用方法
```go
// buildAuthorizedQuery 构建带权限的基础查询
func (s *StatsServiceImpl) buildAuthorizedQuery(currentUser *model.User) *gorm.DB {
    query := s.db.Model(&model.Defect{})

    if !currentUser.IsAdmin() {
        // 普通用户：JOIN桥梁表过滤
        query = query.Joins("JOIN bridges ON bridges.id = defects.bridge_id").
            Where("bridges.user_id = ?", currentUser.ID)
    }

    return query.Where("defects.deleted_at IS NULL")
}
```

### 5.6 验收标准

- ✅ 6个方法全部实现
- ✅ 缓存集成正确（Get → DB → Set）
- ✅ 权限过滤正确（管理员/普通用户）
- ✅ SQL查询优化（使用子查询）
- ✅ 代码编译通过：`go build ./...`

---

## 6. 阶段4：UseCase层实现

### 6.1 任务清单

- [ ] 创建 `stats_usecase.go` 文件
- [ ] 实现参数校验和默认值处理
- [ ] 实现数据转换逻辑
- [ ] 实现6个接口方法

### 6.2 预计时间

**20分钟**

### 6.3 文件路径

```
internal/application/usecase/stats_usecase.go
```

### 6.4 代码结构

```go
// Package usecase 定义应用层用例
package usecase

import (
    "github.com/xinyiis/BridgeDefectDetectionSys/src/backend/internal/application/dto"
    "github.com/xinyiis/BridgeDefectDetectionSys/src/backend/internal/domain/model"
    "github.com/xinyiis/BridgeDefectDetectionSys/src/backend/internal/domain/service"
)

// StatsUseCase 统计用例
type StatsUseCase struct {
    statsService service.StatsService
}

// NewStatsUseCase 创建统计用例实例
func NewStatsUseCase(statsService service.StatsService) *StatsUseCase {
    return &StatsUseCase{statsService: statsService}
}

// GetOverview 获取概览统计
func (uc *StatsUseCase) GetOverview(currentUser *model.User) (*dto.StatsOverview, error) {
    return uc.statsService.GetOverview(currentUser)
}

// GetDefectTypeDistribution 获取缺陷类型分布
func (uc *StatsUseCase) GetDefectTypeDistribution(currentUser *model.User, days int) (*dto.DefectTypeDistributionResponse, error) {
    // 参数校验
    if days < 0 {
        days = 30 // 默认30天
    }
    if days > 365 {
        days = 365 // 最大1年
    }

    return uc.statsService.GetDefectTypeDistribution(currentUser, days)
}

// GetDefectTrend 获取缺陷趋势统计
func (uc *StatsUseCase) GetDefectTrend(currentUser *model.User, days int, granularity string) (*dto.DefectTrendResponse, error) {
    // 参数校验
    if days <= 0 {
        days = 7 // 默认7天
    }
    if granularity != "day" && granularity != "week" && granularity != "month" {
        granularity = "day" // 默认按天
    }

    return uc.statsService.GetDefectTrend(currentUser, days, granularity)
}

// GetBridgeRanking 获取桥梁健康度排名
func (uc *StatsUseCase) GetBridgeRanking(currentUser *model.User, limit int, order string) (*dto.BridgeRankingResponse, error) {
    // 参数校验
    if limit <= 0 {
        limit = 10 // 默认10条
    }
    if limit > 50 {
        limit = 50 // 最大50条
    }
    if order != "worst" && order != "best" {
        order = "worst" // 默认最差优先
    }

    return uc.statsService.GetBridgeRanking(currentUser, limit, order)
}

// GetRecentDetections 获取最近检测记录
func (uc *StatsUseCase) GetRecentDetections(currentUser *model.User, limit int) (*dto.RecentDetectionsResponse, error) {
    // 参数校验
    if limit <= 0 {
        limit = 10 // 默认10条
    }
    if limit > 50 {
        limit = 50 // 最大50条
    }

    return uc.statsService.GetRecentDetections(currentUser, limit)
}

// GetHighRiskAlerts 获取高危缺陷告警
func (uc *StatsUseCase) GetHighRiskAlerts(currentUser *model.User, severity string, limit int) (*dto.HighRiskAlertsResponse, error) {
    // 参数校验
    if limit <= 0 {
        limit = 20 // 默认20条
    }
    if limit > 100 {
        limit = 100 // 最大100条
    }

    return uc.statsService.GetHighRiskAlerts(currentUser, severity, limit)
}
```

### 6.5 验收标准

- ✅ 参数校验完整（默认值、边界检查）
- ✅ 6个方法全部实现
- ✅ 代码编译通过：`go build ./...`

---

## 7. 阶段5：Handler + 路由

### 7.1 任务清单

- [ ] 创建 `stats_handler.go` 文件
- [ ] 实现6个接口方法
- [ ] 注册路由到 `router.go`
- [ ] 依赖注入（Service → UseCase → Handler）

### 7.2 预计时间

**30分钟**

### 7.3 文件路径

```
internal/interfaces/http/handler/stats_handler.go
internal/interfaces/http/router/router.go (修改)
```

### 7.4 Handler实现

```go
// Package handler 定义HTTP请求处理器
package handler

import (
    "strconv"

    "github.com/gin-gonic/gin"
    "github.com/xinyiis/BridgeDefectDetectionSys/src/backend/internal/application/usecase"
    "github.com/xinyiis/BridgeDefectDetectionSys/src/backend/internal/domain/model"
    "github.com/xinyiis/BridgeDefectDetectionSys/src/backend/pkg/response"
)

// StatsHandler 统计接口处理器
type StatsHandler struct {
    statsUseCase *usecase.StatsUseCase
}

// NewStatsHandler 创建统计Handler实例
func NewStatsHandler(statsUseCase *usecase.StatsUseCase) *StatsHandler {
    return &StatsHandler{statsUseCase: statsUseCase}
}

// GetOverview 获取概览统计
// @Summary 获取用户概览统计
// @Tags 统计
// @Produce json
// @Success 200 {object} response.Response
// @Router /api/v1/stats/overview [get]
func (h *StatsHandler) GetOverview(c *gin.Context) {
    currentUser, exists := c.Get("current_user")
    if !exists {
        response.Unauthorized(c)
        return
    }
    user := currentUser.(*model.User)

    data, err := h.statsUseCase.GetOverview(user)
    if err != nil {
        response.InternalErrorWithDetail(c, err.Error())
        return
    }

    response.Success(c, data)
}

// GetDefectTypeDistribution 获取缺陷类型分布
// @Summary 获取缺陷类型分布统计
// @Tags 统计
// @Produce json
// @Param days query int false "统计天数"
// @Success 200 {object} response.Response
// @Router /api/v1/stats/defect-types [get]
func (h *StatsHandler) GetDefectTypeDistribution(c *gin.Context) {
    currentUser, exists := c.Get("current_user")
    if !exists {
        response.Unauthorized(c)
        return
    }
    user := currentUser.(*model.User)

    days, _ := strconv.Atoi(c.DefaultQuery("days", "30"))

    data, err := h.statsUseCase.GetDefectTypeDistribution(user, days)
    if err != nil {
        response.InternalErrorWithDetail(c, err.Error())
        return
    }

    response.Success(c, data)
}

// 其他4个方法类似实现...
```

### 7.5 路由注册

修改 `router.go`：

```go
// setupAPIRoutes 设置API路由
func setupAPIRoutes(r *gin.Engine, db *gorm.DB, cfg *config.Config) {
    // ... 现有依赖注入 ...

    // 统计服务
    statsService := persistence.NewStatsService(db)
    statsUseCase := usecase.NewStatsUseCase(statsService)
    statsHandler := handler.NewStatsHandler(statsUseCase)

    // ... 现有路由 ...
}

// registerAuthRoutes 注册认证路由
func registerAuthRoutes(r *gin.RouterGroup, ..., statsHandler *handler.StatsHandler, ...) {
    // ... 现有路由 ...

    // ========== 统计接口 ==========
    stats := r.Group("/stats")
    {
        stats.GET("/overview", statsHandler.GetOverview)
        stats.GET("/defect-types", statsHandler.GetDefectTypeDistribution)
        stats.GET("/defect-trend", statsHandler.GetDefectTrend)
        stats.GET("/bridge-ranking", statsHandler.GetBridgeRanking)
        stats.GET("/recent-detections", statsHandler.GetRecentDetections)
        stats.GET("/high-risk-alerts", statsHandler.GetHighRiskAlerts)
    }
}
```

### 7.6 验收标准

- ✅ 6个Handler方法实现
- ✅ 路由注册正确
- ✅ 依赖注入正确
- ✅ 代码编译通过：`go build ./...`

---

## 8. 阶段6：数据库索引优化

### 8.1 任务清单

- [ ] 分析现有索引
- [ ] 创建缺陷表索引SQL
- [ ] 执行索引创建
- [ ] 验证索引生效

### 8.2 预计时间

**10分钟**

### 8.3 SQL脚本

```sql
-- ========================================
-- 桥梁缺陷检测系统 - 统计模块索引优化
-- ========================================

USE bridge_detection;

-- 1. 检查现有索引
SHOW INDEX FROM defects;

-- 2. 创建缺陷表索引（如果不存在）

-- 桥梁ID索引（权限过滤）
CREATE INDEX IF NOT EXISTS idx_defects_bridge_id ON defects(bridge_id);

-- 检测时间索引（时间范围查询）
CREATE INDEX IF NOT EXISTS idx_defects_detected_at ON defects(detected_at);

-- 缺陷类型索引（类型分布统计）
CREATE INDEX IF NOT EXISTS idx_defects_type ON defects(defect_type);

-- 软删除索引（过滤已删除数据）
CREATE INDEX IF NOT EXISTS idx_defects_deleted_at ON defects(deleted_at);

-- 组合索引（权限 + 时间查询）
CREATE INDEX IF NOT EXISTS idx_defects_bridge_time ON defects(bridge_id, detected_at);

-- 置信度索引（高危告警查询）
CREATE INDEX IF NOT EXISTS idx_defects_confidence ON defects(confidence);

-- 3. 分析索引效果
EXPLAIN SELECT COUNT(*) FROM defects
WHERE bridge_id = 1
  AND detected_at >= DATE_SUB(CURDATE(), INTERVAL 7 DAY)
  AND deleted_at IS NULL;

-- 4. 查看索引大小
SELECT
    table_name,
    index_name,
    ROUND(stat_value * @@innodb_page_size / 1024 / 1024, 2) AS size_mb
FROM mysql.innodb_index_stats
WHERE table_name = 'defects' AND database_name = 'bridge_detection'
GROUP BY index_name;
```

### 8.4 执行命令

```bash
# 执行索引创建
mysql -uroot -p123456 < doc/stats_indexes.sql

# 或手动执行
mysql -uroot -p123456
> USE bridge_detection;
> SHOW INDEX FROM defects;
```

### 8.5 验收标准

- ✅ 6个索引创建成功
- ✅ EXPLAIN显示使用索引（type=ref或range）
- ✅ 查询速度提升明显（< 100ms）

---

## 9. 阶段7：测试编写

### 9.1 任务清单

- [ ] 创建 `stats_handler_test.go` 文件
- [ ] 编写8个集成测试
- [ ] 运行测试确保全部通过

### 9.2 预计时间

**30分钟**

### 9.3 文件路径

```
internal/interfaces/http/handler/stats_handler_test.go
```

### 9.4 测试用例清单

1. `TestGetOverview_Success` - 获取概览统计成功
2. `TestGetOverview_PermissionFilter` - 权限过滤验证（用户只看自己的）
3. `TestGetDefectTypeDistribution_Success` - 类型分布统计
4. `TestGetDefectTrend_Success` - 趋势统计（7天）
5. `TestGetBridgeRanking_Success` - 桥梁排名（Top 10）
6. `TestGetBridgeRanking_EmptyData` - 无数据情况
7. `TestGetRecentDetections_Success` - 最近检测记录
8. `TestGetHighRiskAlerts_Success` - 高危告警

### 9.5 测试模板

```go
// TestGetOverview_Success 测试获取概览统计成功
func TestGetOverview_Success(t *testing.T) {
    db := setupStatsTestDB(t)
    router := setupStatsTestRouter(db)

    // 创建测试数据
    user := createStatsTestUser(db, "testuser", "user")
    createStatsTestBridge(db, user.ID, "测试桥梁")
    createStatsTestDefect(db, 1, "裂缝")

    cookies := loginStatsTest(t, router, "testuser", "123456")

    // 发送请求
    req := httptest.NewRequest("GET", "/api/v1/stats/overview", nil)
    for _, cookie := range cookies {
        req.AddCookie(cookie)
    }
    w := httptest.NewRecorder()
    router.ServeHTTP(w, req)

    // 验证响应
    if w.Code != http.StatusOK {
        t.Errorf("Expected status 200, got %d: %s", w.Code, w.Body.String())
    }

    var response map[string]interface{}
    json.Unmarshal(w.Body.Bytes(), &response)

    if response["code"].(float64) != 200 {
        t.Errorf("Expected code 200, got %v", response["code"])
    }

    data := response["data"].(map[string]interface{})
    if data["bridge_count"].(float64) != 1 {
        t.Errorf("Expected bridge_count 1, got %v", data["bridge_count"])
    }
}
```

### 9.6 运行测试

```bash
# 运行统计模块测试
go test ./internal/interfaces/http/handler -v -run TestGetOverview -timeout 30s
go test ./internal/interfaces/http/handler -v -run TestGetDefect -timeout 30s

# 运行所有测试
go test ./internal/interfaces/http/handler -v -timeout 30s
```

### 9.7 验收标准

- ✅ 8个测试用例全部通过
- ✅ 覆盖主要功能和边界情况
- ✅ 权限过滤测试通过
- ✅ 测试通过率：100%

---

## 10. 阶段8：提交代码

### 10.1 任务清单

- [ ] 运行完整测试
- [ ] 编译构建验证
- [ ] 查看文件变更
- [ ] 提交代码

### 10.2 预计时间

**5分钟**

### 10.3 执行命令

```bash
# 1. 运行完整测试
go test ./... -timeout 30s

# 2. 编译验证
go build ./...

# 3. 查看变更
git status
git diff

# 4. 提交代码
git add -A
git commit -m "feat: 实现主页统计模块（带内存缓存）

- 实现6个统计接口（概览、类型分布、趋势、排名、检测记录、告警）
- 集成内存缓存（5分钟TTL）
- 数据库索引优化（6个索引）
- 权限过滤（管理员看全局，用户看自己）
- 完整测试套件（8个集成测试，100%通过）
- 健康评分算法和高危告警规则

技术栈：
- Go内存缓存（sync.RWMutex）
- MySQL聚合查询优化
- DDD四层架构

性能优化：
- 缓存命中：< 10ms
- 缓存未命中：< 100ms
- 并发安全，自动过期清理

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```

### 10.4 验收标准

- ✅ 所有测试通过
- ✅ 代码编译通过
- ✅ Git提交成功
- ✅ 提交信息规范

---

## 11. 交付清单

### 11.1 新增文件（8个）

| 文件路径 | 作用 | 代码行数 |
|---------|------|----------|
| `internal/application/dto/stats_dto.go` | DTO定义 | ~150行 |
| `pkg/cache/memory_cache.go` | 内存缓存 | ~200行 |
| `internal/domain/service/stats_service.go` | Service接口 | ~40行 |
| `internal/infrastructure/persistence/stats_service_impl.go` | Service实现 | ~500行 |
| `internal/application/usecase/stats_usecase.go` | UseCase | ~120行 |
| `internal/interfaces/http/handler/stats_handler.go` | Handler | ~180行 |
| `internal/interfaces/http/handler/stats_handler_test.go` | 测试 | ~400行 |
| `doc/stats_indexes.sql` | 索引SQL | ~50行 |

**总计**: ~1640行代码

### 11.2 修改文件（1个）

| 文件路径 | 修改内容 |
|---------|---------|
| `internal/interfaces/http/router/router.go` | 添加统计服务依赖注入和路由注册 |

### 11.3 Git提交统计

```
新增文件: 8个
修改文件: 1个
总计: ~1650行代码
```

---

## 12. 性能验收标准

### 12.1 响应时间要求

| 接口 | 缓存命中 | 缓存未命中 |
|------|---------|-----------|
| 概览统计 | < 10ms | < 100ms |
| 类型分布 | < 10ms | < 150ms |
| 趋势查询 | < 10ms | < 200ms |
| 桥梁排名 | < 10ms | < 150ms |
| 最近检测 | < 10ms | < 100ms |
| 高危告警 | < 10ms | < 150ms |

### 12.2 并发性能

- 并发用户：50人
- 并发请求：100 QPS
- 平均响应时间：< 50ms
- 错误率：< 0.1%

### 12.3 缓存效果

- 缓存命中率：> 80%
- 内存占用：< 10MB
- 缓存清理周期：1分钟
- TTL过期准确性：± 1秒

---

## 13. 开发注意事项

### 13.1 编码规范

1. ✅ 遵循Go官方代码风格
2. ✅ 使用gofmt格式化代码
3. ✅ 注释清晰（中文注释）
4. ✅ 错误处理完整

### 13.2 性能优化

1. ✅ 优先使用子查询而非多次查询
2. ✅ 避免N+1查询问题
3. ✅ 使用JOIN进行权限过滤
4. ✅ 缓存高频查询结果

### 13.3 安全注意事项

1. ✅ 权限过滤贯穿所有接口
2. ✅ 参数校验（边界检查）
3. ✅ SQL注入防护（使用GORM预编译）
4. ✅ 缓存键隔离（按用户ID）

### 13.4 常见问题

**Q1: 内存缓存会不会占用太多内存？**
A: 不会。每个用户的缓存数据 < 10KB，100个用户 < 1MB，可忽略。

**Q2: 服务重启缓存丢失怎么办？**
A: 统计数据不重要，首次查询会自动填充缓存，对用户无感知。

**Q3: 多实例部署缓存不一致？**
A: 小项目单实例部署，如果需要多实例，后续可升级到Redis。

**Q4: 缓存失效策略？**
A: 当前仅依赖TTL自动过期。如需精确失效，可在创建缺陷时调用`cache.StatsCache.DelPattern("stats:*")`。

---

## 14. 总结

### 14.1 技术亮点

✅ **内存缓存**：
- 无需Redis，降低部署复杂度
- 线程安全（sync.RWMutex）
- 自动过期清理（后台goroutine）
- 支持通配符删除

✅ **SQL优化**：
- 子查询合并（3次 → 1次）
- 索引优化（6个关键索引）
- JOIN权限过滤（避免后处理）

✅ **DDD架构**：
- Service → UseCase → Handler三层清晰
- 依赖注入，易于测试
- 缓存逻辑封装在Service层

### 14.2 预期收益

**开发效率**：
- 3.5小时完成全部功能
- 8个测试用例覆盖核心逻辑
- 代码复用性高

**性能提升**：
- 缓存命中率 > 80%，响应时间 < 10ms
- 缓存未命中，响应时间 < 100ms
- 支持100 QPS并发

**用户体验**：
- 主页数据秒开
- 6个统计维度全覆盖
- 告警和排名助力决策

---

**开发计划文档结束**

**准备好了吗？开始开发！** 🚀
